-- Общая переписка роли заводится вместе с самой ролью, а не первым поводом с
-- `session: shared`: поводы разбора живут в своих сессиях и уезжают в архив,
-- и без этой карточки роли на задаче не видно в сайдбаре вовсе. В заводку
-- уходят общие правила — отметка о них, чтобы не слать их в ту же переписку
-- второй раз.
ALTER TABLE aside ADD COLUMN greeted_at TEXT;
