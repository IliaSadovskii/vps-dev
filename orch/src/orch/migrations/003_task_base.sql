-- Ветка, от которой ответвляется задача. Нужна ровно один раз — когда
-- движок создаёт рабочую копию, — но создание копии происходит позже
-- создания задачи, поэтому значение надо где-то пережить. Пусто — база
-- по умолчанию (origin/HEAD, иначе main).
ALTER TABLE task ADD COLUMN base_branch TEXT;
