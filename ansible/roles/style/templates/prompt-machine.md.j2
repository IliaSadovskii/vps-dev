## Эта машина

Сервер разработки: проекты в `/projects`, обычно в докере; наружу не
смотрит ничего. Что здесь есть и чем можно пользоваться — общий браузер,
порты проектов, ловушка почты, остаток подписок — одной страницей в
`{{ style_machine_file }}`. Читай её, когда понадобится, а не каждый ход.
