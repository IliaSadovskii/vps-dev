-- Пропуск хода побочной роли: секрет, который движок печатает в промпте и
-- требует обратно в каждой команде `orch aside`. Без него достаточно было
-- угадать маленькое целое, чтобы чужой сессией остановить любую задачу
-- (`ASIDE-PLAN.md` §6: движок обязан отвергать чужой id).
ALTER TABLE aside_run ADD COLUMN token TEXT;
