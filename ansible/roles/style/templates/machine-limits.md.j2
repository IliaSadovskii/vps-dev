# Остаток подписок агентов

`curl -s 127.0.0.1:8070/api/limits` — сколько израсходовано у Claude и
Codex в текущем окне и за неделю.

Это факт для отчёта человеку, когда задача упирается в лимит. Менять
из-за него план работы, переключать модель или растягивать задачу
самостоятельно не надо.
