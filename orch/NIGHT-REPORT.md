# Ночь 2026-09-06: что построено и что из этого правда

Отчёт разработчика orch по `HANDOFF.md`. Этапы 0–5 закрыты, настоящая задача
доведена до приёмки. Ниже — что работает, что проверено живьём и чему верить
нельзя.

## Что верно сразу, до всего остального

**Все задачи ночи шли на Opus, а не на Sonnet.** `agent_model` при создании
сессии AoE отдаёт адаптеру переменной окружения, которую `claude-agent-acp`
не читает. Нашёл владелец, движок починен и проверен, но пробный период на
дешёвой модели ещё не проверялся ни разу. Всё, что ниже про качество ролей,
длительность ходов и число возвратов, — про Opus.

## Этапы

| Этап | Критерий `PLAN.md` §10 | Итог |
|---|---|---|
| 0. Спайк | шесть проверок скриптами с записью в `RISKS.md` | закрыт: `orch/spike/`, все шесть `ok` |
| 1. Ядро без панели | `smoke` на полигоне в четырёх режимах, юнит-тесты зелёные | закрыт: четыре прогона, 94 теста |
| 2. Панель | `smoke` целиком с телефона через панель | закрыт: весь путь пройден кликами в браузере |
| 3. Conventions | с воротами и без, промахи промптов в `PROMPT-NOTES.md` | закрыт: два прогона |
| 4. Deep | маленькая задача на полигоне + настоящая по `FIRST-TASK.md` | закрыт: обе, отчёт — `FIRST-TASK-REPORT.md` |
| 5. Выкатка | роль Ansible `orch` | закрыт: `ansible/roles/orch`, в `site.yml` после `aoe` |

## Чего это стоило: 51 заход, 6,9 часа в ходах, 12 задач

Из них настоящая — одна (18 заходов, 5,6 ч). Остальные — полигон и проверки
механики. Стоимость движок не собрал: `UsageUpdated` в этой версии AoE отдаёт
`cost: null`.

## Дефекты, найденные только живым прогоном

Ни один из них не поймали бы юнит-тесты — все нашлись на живом AoE или на
настоящей задаче.

**Тихие и опасные:**

1. Папка задачи писалась в основной репозиторий вместо рабочей копии сессии.
2. `context: continue` молча заводил чистую сессию: маршрута `GET /api/sessions/{id}`
   в AoE нет вовсе, и поиск прошлой сессии всегда возвращал пусто. Поймала сама
   роль, честно написав «памяти нет».
3. Архив звался POST вместо PATCH — 405, и ничего не архивировалось.
4. **Модель сессии не применялась вообще** (см. выше).
5. Роли подагентов искались маской по имени шага: для `code-review` маска
   `sub-code*` не находила `sub-review-*.md`, и роль осталась бы без путей.

**Ломающие поток:**

6. Время сравнивалось строками: `...Z` против `+00:00` — конец хода не
   отличался от его начала.
7. Уснувший воркер будился каждые пять секунд без конца.
8. Ключ идемпотентности без времени задачи воскрешал мёртвую сессию после
   пересоздания базы.
9. Клик приходит в своём потоке, а соединение SQLite привязано к своему —
   любая кнопка панели падала.
10. Задача, вставшая на вопросе роли, не снималась ответом владельца: движок
    переставал смотреть на сессию, задача висела бы вечно.
11. «Ещё заход» на пределе заходов не поднимал предел — кнопка в пустоту.
12. «Принять как есть» повторяла тот исход, который и водил задачу по кругу.
13. Имя ветки в базе получало лишний дефис от обрезки — `orch push` отвечал
    «src refspec does not match any».
14. Черновик новой задачи хранился на сессию, а клики общей панели приходят
    без `session_id`.
15. Очистка поля ввода отдельной посылкой затиралась перерисовкой.

**Дефект цепочки, а не кода:** ревью кода могло вернуть находку про тесты
только в Правки, а тесты правит одна Реализация — задача ходила по кругу до
предела заходов. Ревью само это и написало. Добавлен исход
`tests → implementation`; на настоящей задаче он сработал дважды.

**Дефект интерфейса, найденный владельцем:** сайдбар сворачивал все сессии
задачи в одну строку с именем ветки. Теперь рабочую копию делает движок, а
сессии создаются обычными — каждая своей строкой с титулом.

## Что осталось владельцу

1. **Один раз перезапустить `aoe serve` на 8065**, чтобы панель появилась в
   рабочем дашборде. Роль Ansible этого не делает намеренно: рестарт оборвёт
   живые сессии. Дальше правки подхватываются через `plugin disable/enable`.
2. **Ничего с `gh` делать не нужно** — он авторизован. Правка отчёта после
   проверки: роль PR видела «не залогинен» потому, что задача шла на запасном
   демоне с `XDG_CONFIG_HOME=/tmp/orch-spike/.config`, а `gh` ищет `hosts.yml`
   там. На рабочем демоне push проходит. Задачи, которым нужен push или PR,
   гонять только на рабочем демоне.
3. **Прогнать одну задачу на Sonnet** — до этого о пробном периоде говорить
   нечего.
4. Решить, что делать с `max_runs: 3` у Ревью кода: четырёх заходов на
   настоящей задаче не хватило дважды.

## Решения, принятые без владельца

Восемь штук, все в `DECISIONS-NEEDED.md` с вариантами и причиной: отдельный
демон для разработки панели, длительность проверки висящего вопроса, папка
`logs/` в задаче, кнопка у поля ввода прикрепляет текст вместо движения,
адрес демона как настройка плагина, сторож молчащего хода AoE, рабочие копии
силами движка, применение модели вызовом `config-option`.

## Где что смотреть

- Спецификация и решения: `orch/PLAN.md`, `orch/DECISIONS-NEEDED.md`.
- Что нашёл спайк: `orch/research/RISKS.md`, раздел «Результаты спайка».
- Как вели себя роли: `orch/PROMPT-NOTES.md`.
- Настоящая задача: `orch/FIRST-TASK-REPORT.md`.
- Код: `orch/src/orch/` (движок `engine.py`, панели `panels.py`, клиент
  `aoe.py`), тесты `orch/tests/`, роль выкатки `ansible/roles/orch/`.
