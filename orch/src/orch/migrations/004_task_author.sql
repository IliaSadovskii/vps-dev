-- Кто завёл задачу. Роли имеют право класть заявки в бэклог (`PLAN.md` §6),
-- и в панели такая заявка неотличима от заведённой владельцем. Пусто —
-- завёл владелец; иначе id задачи, чья роль это сделала.
ALTER TABLE task ADD COLUMN author TEXT;
