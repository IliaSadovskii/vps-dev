-- Заходы, забытые возвратом «начисто»: их сессии больше не подхватываются
-- шагами с памятью (`context: own` и `continue`), а их артефакты уехали в
-- историю. Строка остаётся: путь задачи по шагам не переписывается задним
-- числом.
ALTER TABLE run ADD COLUMN void_at TEXT;
