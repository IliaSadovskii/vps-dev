-- Сессия мастера, в которой заводили задачу. Она уходит в архив сразу после
-- создания задачи: рядом с ходами её в сайдбаре не поставить (группировка
-- идёт по каталогу, а мастер живёт в проекте, шаги — в рабочей копии).
-- Разговор о постановке от этого не теряется: панель задачи даёт на него
-- ссылку по этому полю.
ALTER TABLE task ADD COLUMN wizard_session TEXT;
