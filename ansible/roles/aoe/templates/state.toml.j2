# Заготовка от Ansible: roles/aoe/templates/state.toml.j2 — кладётся один раз.
# Служебное состояние aoe; нам нужна одна строка: откуда начинать обзор
# папок в диалоге новой сессии. Дальше aoe ведёт файл сам.
last_browse_dir = "{{ base_projects_dir }}"
