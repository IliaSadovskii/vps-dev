-- Уведомлять ли владельца в Telegram, когда эта задача встала на воротах.
-- Свойство задачи, а не машины: одну задачу владелец ведёт с телефона и
-- хочет знать сразу, другая едет фоном и звать не должна.
ALTER TABLE task ADD COLUMN notify_gates INTEGER NOT NULL DEFAULT 0;
