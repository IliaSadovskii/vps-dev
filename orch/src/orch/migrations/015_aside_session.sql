-- Одна сессия побочной роли на всю задачу: роль просыпается новым промптом в
-- ту же сессию, а не заводит по сессии на каждый повод. Владелец читает её
-- отчёт в сайдбаре и закрывает сам, когда разберётся.
ALTER TABLE aside ADD COLUMN session_id TEXT;
